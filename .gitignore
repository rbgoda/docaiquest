node_modules/
dist/
__pycache__/
*.pyc
.env
.venv/

.DS_Store

# Generated eval datasets may contain real document PII — never commit.
# The hand-authored seed (eval/dataset/ragas_qa.json) is safe + tracked.
eval/dataset/testdata_qa.json
eval/dataset/live_qa.json

# POC test fixtures — real docs + extracted metadata (PII). Local-only.
eval/fixtures/

# generated local eval report
eval/dashboard.html

# Claude Code internal files — never commit
.claude/

# Prod deploy scripts — host-specific, not for OSS
deploy.sh
scripts/prod_postactivation_check.sh


# Claude Code memory — never commit
MEMORY.md
